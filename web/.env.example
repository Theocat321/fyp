# Frontend env example (copy to web/.env.local)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_USE_STREAMING=true

# Optional: Supabase client (frontend). Provide anon key if you want
# the UI to persist messages to your project's table. Configure RLS
# appropriately to allow inserts from anon role.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
