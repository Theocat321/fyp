# Unified environment example for local development
#
# This file documents all environment variables used across the repo
# (Next.js app in `web/` and optional FastAPI server in `server/`).
#
# Usage:
# - Do NOT put secrets here. Keep this file committed as an example only.
# - For local Next.js, create `web/.env.local` and copy relevant values.
# - For local FastAPI, create `server/.env` and copy relevant values.
# - On Vercel, define variables in Project Settings → Environment Variables
#   and use `vercel env pull web/.env.local` to sync local dev.

########################
# Frontend (Next.js)
########################

# Call a separate backend only in local development (leave unset on Vercel)
# When unset, the UI uses built-in Next.js API routes (relative /api/*)
NEXT_PUBLIC_API_BASE_URL=

# Enable streaming UX in the browser
NEXT_PUBLIC_USE_STREAMING=true

# Optional: Supabase client for browser-side persistence
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: Branding and assistant behavior (also read by server routes)
PROVIDER_NAME=VodaCare
ASSISTANT_MODE=open   # open | strict

# Optional: Server-side Supabase (used by Next.js API routes)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: OpenAI for Next.js API routes (server-side only)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=

########################
# Backend (FastAPI - optional)
########################

# CORS and server port for local FastAPI
ALLOWED_ORIGINS=http://localhost:3000
API_PORT=8000

# Reuses the same assistant + OpenAI variables when using FastAPI
# PROVIDER_NAME=
# ASSISTANT_MODE=
# OPENAI_API_KEY=
# OPENAI_MODEL=
# OPENAI_BASE_URL=
