# FastAPI env example (copy to server/.env)

PROVIDER_NAME=VodaCare
ALLOWED_ORIGINS=http://localhost:3000
API_PORT=8000

# Optional: OpenAI SDK (backend-only). If set, replies use the model.
# NEVER expose API keys to the frontend.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=
